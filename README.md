# Slock - the simple screen locker
Simple screen locker utility for X.

## Dependencies
In order to build slock you need the Xlib header files.

## Installation
```sh
git clone git@github.com:shoumodip/slock
make
sudo make install
```
